import { colors } from "../styles/colors";

export const COLORS = colors;

export const SIZES = {
	icon: {
		sm: 16,
		md: 20,
		lg: 24,
		xl: 28,
	},
	image: {
		card: 60,
		detail: 150,
	},
} as const;
